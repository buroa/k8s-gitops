---
apiVersion: v1
kind: Pod
metadata:
  name: disk-wipe-w1
  namespace: rook-ceph
spec:
  restartPolicy: Never
  nodeName: w1
  containers:
    - name: disk-wipe
      image: cmd.cat/sgdisk/fdisk/dd/partprobe
      securityContext:
        privileged: true
      command: ["sleep", "3600"]
      # args:
      #   - sgdisk --zap-all /dev/nvme1n1;
      #     dd if=/dev/zero of=/dev/nvme1n1 bs=4096k count=100 oflag=direct,dsync;
      #     blkdiscard /dev/nvme1n1;
      #     partprobe /dev/nvme1n1;
