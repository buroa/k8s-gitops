---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hypyr
spec:
  secretName: hypyr-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: hypyr.space
  dnsNames: ["hypyr.space", "*.hypyr.space"]
