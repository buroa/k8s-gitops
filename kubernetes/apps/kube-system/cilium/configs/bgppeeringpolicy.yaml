---
apiVersion: cilium.io/v2alpha1
kind: CiliumBGPPeeringPolicy
metadata:
  name: peering-policy
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/os: linux
  virtualRouters:
    - localASN: 64513
      exportPodCIDR: false
      serviceSelector:
        matchLabels:
          io.cilium/lb-ipam-layer: layer3
      neighbors:
        - peerAddress: ${LAN_TRUSTED_GATEWAY_IP}/32
          peerASN: 64513
