---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrypted-recs
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 250Gi
  storageClassName: ${CLUSTER_STORAGE_BLOCK}
