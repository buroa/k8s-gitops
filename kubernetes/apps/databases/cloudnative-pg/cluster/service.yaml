---
apiVersion: v1
kind: Service
metadata:
  name: postgres-lb
  namespace: databases
  annotations:
    coredns.io/hostname: postgres.${SECRET_PUBLIC_DOMAIN}
    io.cilium/lb-ipam-ips: ${POSTGRES_LB_IP}
spec:
  type: LoadBalancer
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    postgresql: postgres
    role: primary
