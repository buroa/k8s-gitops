---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: glauth-config-tpl
    files:
      - ./configs/groups.toml
      - ./configs/server.toml
      - ./configs/users.toml
generatorOptions:
  disableNameSuffixHash: true
