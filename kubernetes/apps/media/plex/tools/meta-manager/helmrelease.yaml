---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: &app plex-meta-manager
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      main:
        type: cronjob
        annotations:
          reloader.stakater.com/auto: "true"
        cronjob:
          schedule: 0 0 * * *
          timeZone: &timeZone ${TIMEZONE}
        containers:
          main:
            image:
              # TODO: move to semver once available
              repository: docker.io/meisnate12/plex-meta-manager
              tag: develop@sha256:688d81677ac5078a5dec6b5978d01760cbf9d2b8a8e0799b0cb3fdc7937c643a
            command:
              - /scripts/run.sh
            env:
              TZ: *timeZone
            envFrom:
              - secretRef:
                  name: plex-meta-manager-secret
            resources:
              requests:
                cpu: 10m
                memory: 4096M
              limits:
                memory: 8192M
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
        pod:
          securityContext:
            runAsUser: 568
            runAsGroup: 568
            runAsNonRoot: true
            fsGroup: 568
            fsGroupChangePolicy: OnRootMismatch
    persistence:
      config:
        enabled: true
        existingClaim: *app
      config-file:
        type: configMap
        name: plex-meta-manager-configmap
        globalMounts:
          - path: /config/config.yml
            subPath: config.yml
            readOnly: true
      config-prerolls:
        type: configMap
        name: plex-meta-manager-configmap
        globalMounts:
          - path: /config/Pre-rolls.yml
            subPath: Pre-rolls.yml
            readOnly: true
      logs:
        type: emptyDir
        globalMounts:
          - path: /config/logs
            subPath: logs
      scripts:
        type: configMap
        name: plex-meta-manager-configmap
        defaultMode: 0775
        globalMounts:
          - path: /scripts/run.sh
            subPath: run.sh
            readOnly: true
    service:
      main:
        enabled: false
