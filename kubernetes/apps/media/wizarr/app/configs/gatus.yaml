---
endpoints:
  - name: wizarr
    url: https://invite.${SECRET_PUBLIC_DOMAIN}/setup
    client:
      dns-resolver: "tcp://1.1.1.1:53"
    interval: 1m
    conditions: ["[STATUS] == 200"]
    alerts: [{ type: pushover }]
