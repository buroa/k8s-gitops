---
endpoints:
  - name: overseerr
    url: https://requests.${SECRET_PUBLIC_DOMAIN}/api/v1/status
    client:
      dns-resolver: "tcp://1.1.1.1:53"
    interval: 1m
    conditions: ["[STATUS] == 200"]
    alerts: [{ type: pushover }]
