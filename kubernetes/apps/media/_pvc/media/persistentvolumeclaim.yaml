---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
spec:
  storageClassName: media
  volumeName: media
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Mi
