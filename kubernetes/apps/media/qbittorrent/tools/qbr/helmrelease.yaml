---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qbr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      qbr:
        containers:
          app:
            image:
              repository: ghcr.io/buroa/qbr
              tag: 0.0.9@sha256:5d3947a77a91bc56f2a81fda3c9dd02ef6ba7feff1ac278e2577c53e41e9a5ec
            env:
              QBITTORRENT_HOST: http://qbittorrent.media.svc.cluster.local:8080
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 32Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
