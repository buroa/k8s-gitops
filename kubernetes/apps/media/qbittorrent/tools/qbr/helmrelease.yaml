---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: qbr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      qbr:
        containers:
          app:
            image:
              repository: ghcr.io/buroa/qbr
              tag: 0.0.3@sha256:99820dc6f584f0e9abc12db7ea056936e5bd9f01b59f1b67a13ea867f8d0d31e
            env:
              QBITTORRENT_HOST: http://qbittorrent.media.svc.cluster.local:8080
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 32Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
