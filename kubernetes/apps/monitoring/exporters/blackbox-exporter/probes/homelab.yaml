---
kind: Probe
apiVersion: monitoring.coreos.com/v1
metadata:
  name: homelab
spec:
  module: icmp
  prober:
    url: blackbox-exporter.monitoring.svc.cluster.local:9115
  targets:
    staticConfig:
      static:
        - nas.internal
        - unifi.internal
        - ups.internal
        - wan1.internal # WAN1 - XFINITY PON ONT
        - wan2.internal # WAN2 - T-MOBILE 4G/5G
