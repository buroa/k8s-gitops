---
warp-routing:
  enabled: true

originRequest:
  http2Origin: true

ingress:
  - hostname: "*.${SECRET_PUBLIC_DOMAIN}"
    service: https://contour-envoy.networking.svc.cluster.local

  - service: http_status:404
