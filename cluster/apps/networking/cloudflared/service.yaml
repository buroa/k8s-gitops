---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/instance: cloudflared
  name: cloudflared
  namespace: networking
spec:
  ports:
    - name: metrics
      port: 2000
      protocol: TCP
      targetPort: metrics
  selector:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/instance: cloudflared
  type: ClusterIP
