---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: omegabrr
    app.kubernetes.io/name: omegabrr
  name: omegabrr
  namespace: media
spec:
  ports:
    - name: http
      port: 7441
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: omegabrr
    app.kubernetes.io/name: omegabrr
  type: ClusterIP
