---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-cache
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 250Gi
  storageClassName: ceph-filesystem
