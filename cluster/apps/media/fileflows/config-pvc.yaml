---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fileflows-config
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-filesystem
