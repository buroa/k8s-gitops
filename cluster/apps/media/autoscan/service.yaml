apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: autoscan
    app.kubernetes.io/name: autoscan
  name: autoscan
  namespace: media
spec:
  ports:
    - name: http
      port: 3030
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: autoscan
    app.kubernetes.io/name: autoscan
  type: ClusterIP
