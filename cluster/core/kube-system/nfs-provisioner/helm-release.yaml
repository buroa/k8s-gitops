---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nfs-provisioner
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://openebs.github.io/dynamic-nfs-provisioner
      chart: nfs-provisioner
      version: 0.9.0
      sourceRef:
        kind: HelmRepository
        name: openebs-nfs-charts
        namespace: flux-system
      interval: 5m
