---
name: Helm Repository Sync

on:
  pull_request:
    branches: ["master"]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  pre-job:
    name: Helm Repository Sync Pre-Job
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    outputs:
      any_changed: ${{ steps.changed-files.outputs.any_changed }}
    steps:
      - name: Get Changed Files
        id: changed-files
        uses: tj-actions/changed-files@v45
        with:
          files: kubernetes/**/helmrelease.yaml

  helm-repository-sync:
    needs: pre-job
    name: Helm Repository Sync
    runs-on: k8s-gitops-runner
    if: ${{ needs.pre-job.outputs.any_changed == 'true' }}
    steps:
      - name: Install flux
        run: curl -fsSL https://fluxcd.io/install.sh | bash

      - name: Sync Helm Repository
        env:
          FILES: ${{ needs.pre-job.outputs.all_changed_and_modified_files }}
        run: |
          yq '.spec.chart.spec.sourceRef | pick(["name","namespace"])' ${FILES} \
              | yq --no-doc '.namespace + " " + .name' | uniq \
              | xargs -l bash -c 'flux --namespace $0 reconcile source helm $1'

  helm-repository-sync-status:
    needs: helm-repository-sync
    name: Helm Repository Sync Success
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - name: Any jobs failed?
        if: ${{ contains(needs.*.result, 'failure') }}
        run: exit 1
      - name: All jobs passed or skipped
        if: ${{ !(contains(needs.*.result, 'failure')) }}
        run: echo "All jobs passed or skipped" && echo "${{ toJSON(needs.*.result) }}"
