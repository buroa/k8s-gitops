router bgp 64513
 bgp router-id 10.0.5.1
 !
 neighbor KUBERNETES peer-group
 neighbor KUBERNETES remote-as 64514
 neighbor KUBERNETES route-map K8S_IN in
 neighbor KUBERNETES route-map K8S_OUT out
 bgp listen range 10.0.5.0/24 peer-group KUBERNETES
 !
 address-family ipv4 unicast
  neighbor KUBERNETES activate
  neighbor KUBERNETES soft-reconfiguration inbound
 exit-address-family
exit
!
ip prefix-list K8S_LB_IPS seq 10 permit 10.0.48.0/24 le 32
!
route-map K8S_IN permit 10
 match ip address prefix-list K8S_LB_IPS
exit
!
route-map K8S_OUT deny 10
exit
!